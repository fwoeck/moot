"""Spoke-side client library: hub connection, home resolution, rendering,
the control socket every runtime spoke serves, and the human observer seat.

Stdlib only — a spoke must run inside whatever process the agent runtime
gives it (see docs/SPOKE-GUIDE.md).
"""
